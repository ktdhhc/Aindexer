{
  "name": "aindexer-desktop-v4",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "sync:version": "node scripts/sync-version.mjs",
    "dev:tauri": "npm run sync:version && tauri dev",
    "build": "node scripts/build.mjs",
    "build:tauri": "npm run sync:version && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.5.0"
  }
}
