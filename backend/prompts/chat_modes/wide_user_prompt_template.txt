任务：根据当前工作区的文献索引上下文，回答用户关于筛选、比较、聚合的问题。

要求：
1. 只依据给定上下文回答。
2. 优先总结候选文献、共性、差异和筛选理由。
3. 如果涉及多篇文献，请尽量按 `[I-01]`、`[I-02]` 这样的索引编号标注依据。
4. 如果用户使用“第一篇 / 第二篇”这类顺序指代，请按 `可用文献顺序` 中的 `[I-xx]` 解释。
5. 当用户说“上一轮 / 刚才 / 继续 / 第三点”时，先结合下面的对话历史理解，再回答当前问题。
6. 如果上下文被压缩或回退为摘要，不要臆造摘要中没有的信息。
7. 回答正文结束后，单独追加一行 `引用索引：I-01, I-03`；如果正文没有直接引用任何索引，则输出 `引用索引：无`。

工作区上下文：
{context}

对话历史（按时间从早到晚）：
{history}

用户问题：
{question}
