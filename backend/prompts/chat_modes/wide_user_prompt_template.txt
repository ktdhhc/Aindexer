任务：根据当前工作区的文献索引上下文，回答用户关于筛选、比较、聚合的问题。

要求：
1. 只依据给定上下文回答。
2. 优先总结候选文献、共性、差异和筛选理由。
3. 如果涉及多篇文献，请尽量按 `[S01]`、`[S02]` 这样的 source 编号标注依据。
4. 如果用户使用“第一篇 / 第二篇”这类顺序指代，请按 `可用文献顺序` 中的 `[Sxx]` 解释。
5. 当用户说“上一轮 / 刚才 / 继续 / 第三点”时，先结合下面的对话历史理解，再回答当前问题。
6. 如果上下文被压缩或回退为摘要，不要臆造摘要中没有的信息。

对话历史（按时间从早到晚）：
{history}

用户问题：
{question}

工作区上下文：
{context}
