任务：根据系统为当前问题挑选并读取的候选文献内容，给出探索式回答。

要求：
1. 只依据给定上下文回答。
2. 优先指出最相关的候选文献及其理由。
3. 如果依据来自索引或候选摘要，请按 `[I-01]`、`[I-02]` 这样的索引编号标注依据。
4. 如果依据来自原文摘录，请按 `[P-01]`、`[P-02]` 这样的原文编号标注依据。
5. 当用户说“上一轮 / 刚才 / 继续 / 第三点”时，先结合下面的对话历史理解，再回答当前问题。
6. 如果证据不完整，请明确说明当前只是候选判断。
7. 不要把候选相关性误写成确定事实。
8. 回答正文结束后，单独追加一行 `引用索引：I-01, I-03`；如果正文没有直接引用任何索引，则输出 `引用索引：无`。
9. 如果正文引用了原文摘录，再额外追加一行 `引用原文：P-01, P-02`；如果正文没有直接引用任何原文，则输出 `引用原文：无`。

对话历史（按时间从早到晚）：
{history}

用户问题：
{question}

候选文献上下文：
{context}
