元数据层：
{metadata_context}

已读取索引层：
{index_context}

已读取正文层：
{paper_context}

对话历史：
{history}

用户问题：
{question}

当前轮次：{iteration}/{max_iterations}
剩余轮次：{remaining_iterations}
本轮是否包含全量元数据：{metadata_visible}
当前已注入元数据条数：{metadata_count}
正文单次读取上限：{paper_top_k}

已执行步骤：
{trace}

决策提醒：
- 如果本轮是否包含全量元数据=是，说明上面的“元数据层”就是当前可见的全库清单；不要声称“看不到元数据”，也不要重复为同一批元数据返回 read_metadata。
- 如果本轮已注入全量元数据，而用户问题不是单纯“列候选/找相关文献”，你应立即从元数据中挑选最相关的 doc_id 并返回 read_index，而不是停留在元数据层观望。
- 当问题涉及比较、方法、贡献、结论、局限、定义、实验、证据、原理、差异、优缺点时，默认应积极读取，而不是保守回答。
- 如果元数据已经足够定位 1-4 篇高相关文献，优先一次性 read_index 这些文献，不要一篇一篇过度保守地试探。
- 如果索引内容还不足以支持证据级回答，应继续主动 read_paper，不要只因为“已有一些信息”就过早 answer。
- 如果上下文中没有全量元数据，而你需要在全库重新找文献，你必须返回 read_metadata。
- 如果你已经知道候选 doc_id，优先 read_index。
- 如果问题涉及观点、方法、定义、比较、结论、局限、实验或证据，不要只停留在元数据层，优先 read_index。
- 只有索引不足时才 read_paper。
- read_index / read_paper 的 doc_ids 不能凭空猜测，必须来自可见元数据或已读取内容。
- 本轮必须输出合法 `action`，绝不能留空。
- 如果不确定，优先选择更保守的读取动作，而不是输出空 JSON。

请只输出 JSON：
{{
  "action": "answer | read_metadata | read_index | read_paper | not_found",
  "reason": "一句话说明",
  "doc_ids": ["doc_id"],
  "answer": "如果 action 是 answer/not_found，可给出草稿，否则留空"
}}

再次检查：
- 不要输出 `{{}}`
- 不要输出 `"action": ""`
- 不要输出 JSON 之外的任何文字
